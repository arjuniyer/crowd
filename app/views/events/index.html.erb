<h1>Listing events</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Are you going?</th>
    <th>When does it start?</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

    
<% @fb_events.each do |event| %>
  <tr>
    <td><%= event['name'] %></td>
    <td><%= event['rsvp_status'] %></td>
    <td><%= event['start_time'] %></td>
    <td><%= link_to 'Get Crowd Information', {:controller => "events", :action => "new", :fb_event_id => event['id'] } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Event', new_event_path %>


